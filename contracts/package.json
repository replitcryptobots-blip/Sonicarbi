{
  "name": "sonicarbi-contracts",
  "version": "1.0.0",
  "description": "Flashloan arbitrage smart contracts for Scroll",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network scrollTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network scroll",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@aave/core-v3": "^1.19.0",
    "@openzeppelin/contracts": "^5.0.0"
  }
}
